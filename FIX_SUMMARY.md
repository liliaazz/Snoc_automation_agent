# Fix Summary

## Modified implementation

- `.env`, `.env.example`: root `.env` confirmed active; documented keys merged;
  exact Qwen/vLLM configuration preserved; example credential placeholder
  sanitized.
- `pyproject.toml`: constrained FastAPI to the tested stable minor line.
- `src/snoc_agent/datetime_utils.py`, `db/base.py`: centralized aware-UTC creation,
  SQLite-naive normalization, serialization, and elapsed-time calculation.
- `src/snoc_agent/ai/intent_safety.py`: deterministic negation, history,
  unsupported-intent, and prompt-injection boundary.
- `src/snoc_agent/workflow/inbound_processor.py`: safety before materialization,
  audit reasons, and canonical body/new-Message-ID duplicate detection.
- `src/snoc_agent/ai/openai_compatible_backend.py`: bounded structured-output
  fallback/repair while preserving provider/model attribution.
- `src/snoc_agent/evaluation/vllm_smoke.py`, `smoke_utils.py`, `metrics.py`: measured
  call latency, selected-deployment discovery, production safety sequence, valid
  empty-operation scoring, and auditable Qwen results.
- `src/snoc_agent/api/app.py`, `api/queries.py`, `api/dashboard_router.py`: stable
  frontend contracts, request/email/operation count separation, UTC fixes,
  password redaction, truthful model metrics, trace/runtime/analytics routes, and
  period/range validation.
- `dashboard.py` and two Alembic migration files: formatting-only changes required
  by the repository format gate.

## Added/updated verification

- `scripts/run_backend_acceptance.sh`: isolated DB, migrations, deterministic
  backend tests, Uvicorn/Vite startup, and proxy API smoke.
- `scripts/run_qwen_acceptance.sh`: exact provider/model preflight, isolated DB,
  real Qwen analyzer/verifier calls, no demo fallback, structured-output and unsafe
  auto-execution gates.
- `tests/fixtures/emails/hardening_matrix.json`: exact 50-case traceability set.
- New tests for intent safety, datetime normalization, traceability, 50+20 batch
  concurrency, API contracts, populated secret-free trace, adapter status/retry
  policy, malformed structured output, duplicate bodies, and negated E2E mail.
- Existing robustness test now pins the legacy engine it is specifically testing,
  so active `.env` workflow selection cannot change its subject.

## Commands executed

```bash
python -m ruff format --check .
python -m ruff check .
python -m mypy src
python -m pytest -q
cd frontend && npm install && npm run build
cd ..
./scripts/run_backend_acceptance.sh
./scripts/run_qwen_acceptance.sh
```

Because the host filesystem filled while retaining the mandatory 1.2 GB backup,
final Ruff/mypy caches were directed to `/tmp`; this does not alter results.

## Results

- Full pytest: 337 collected, 332 passed, 5 intentionally skipped live-external
  tests, 0 failed.
- Ruff format/check: pass (469 files formatted; no lint violations).
- mypy: pass (`src`, 128 source files).
- Frontend install/build: pass; Vite reports a non-fatal 875.37 kB chunk warning.
- Backend/API/Vite-proxy acceptance: pass on isolated SQLite at
  `/tmp/snoc_backend_acceptance_20260726T105320Z.db`.
- Qwen exact model: verified; no demo fallback; DRY_RUN; 90% exact semantics,
  100% structured validity after bounded repair, zero unsafe guarded proposals.
- Final Qwen artifact:
  `outputs/qwen_acceptance_20260726T104625Z/smoke_report.json`.

## Backup and diff

Git metadata was unavailable (the `.git` path did not contain a usable
repository), so no branch or Git diff exists. The complete pre-hardening backup is:

`/tmp/snoc_automation_agents-integration_backup_20260726T093104Z/project`

Changes were audited with a recursive comparison against that backup. No frontend
source or visual file changed; `frontend/dist` was regenerated by the required
build only.
