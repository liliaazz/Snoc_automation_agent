[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "snoc-integrated-agent"
version = "1.0.0"
description = "Integrated SNOC agent: LangGraph pipeline + SVM hybrid classifier + FastAPI dashboard"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "alembic>=1.14,<2",
  "fastapi>=0.115,<0.116",
  "httpx>=0.27,<1",
  "langchain>=1.3,<2",
  "langchain-openai>=1.4,<2",
  "langgraph>=1.2,<2",
  "pydantic>=2.10,<3",
  "pydantic-settings>=2.6,<3",
  "psycopg[binary]>=3.2,<4",
  "python-dotenv>=1.0,<2",
  "scikit-learn>=1.5,<2",
  "PyJWT[crypto]>=2.9,<3",
  "slowapi>=0.1.9,<1",
  "SQLAlchemy>=2.0,<3",
  "uvicorn[standard]>=0.30,<1",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.13,<2",
  "pytest>=8.3,<9",
  "pytest-cov>=6,<7",
  "ruff>=0.8,<1",
]
dashboard = [
  "pandas>=2.2,<4",
  "streamlit>=1.40,<2",
]
postgres-checkpoint = [
  "langgraph-checkpoint-postgres>=3.1,<4",
]

[project.scripts]
snoc-agent = "snoc_agent.cli.main:main"
snoc-api = "snoc_agent.run_api:main"
snoc-seed = "snoc_agent.seed:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
snoc_agent = ["prompts/*.md"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
markers = [
  "local_model: requires a configured local OpenAI-compatible model server",
  "vllm_live: requires VLLM_API_KEY and RUN_VLLM_LIVE_TESTS=true; performs tiny fake-data-only calls",
]

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [
  ".review",
  "data",
  "deliverables",
  "labeled data",
  "labeled_data",
  "notebooks",
  "reports",
  "scripts",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.12"
packages = ["snoc_agent"]
strict = false
warn_unused_ignores = true
warn_redundant_casts = true
